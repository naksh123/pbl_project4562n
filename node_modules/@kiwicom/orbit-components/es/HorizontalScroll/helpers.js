export function throttle(fn, time) {
  var lastTime = 0;
  return function () {
    var now = new Date();

    if (now - lastTime >= time) {
      fn.apply(void 0, arguments);
      lastTime = now;
    }
  };
}