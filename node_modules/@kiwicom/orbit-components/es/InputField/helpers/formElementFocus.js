import { css } from "styled-components";
import convertHexToRgba from "@kiwicom/orbit-design-tokens/lib/convertHexToRgba";

var formElementFocus = function formElementFocus() {
  return css(["box-shadow:", ";"], function (_ref) {
    var theme = _ref.theme,
        error = _ref.error;
    return error ? // TODO: UPDATE TOKENS ==> `${theme.orbit.borderWidthInputFocus} ${theme.orbit.borderColorInputFocus}`
    "inset 0 0 0 1px ".concat(theme.orbit.paletteRedNormal, ", 0 0 0 3px ").concat(convertHexToRgba(theme.orbit.paletteRedNormal, 15), ";") : "inset 0 0 0 1px ".concat(theme.orbit.borderColorInputFocus, ", 0 0 0 3px ").concat(convertHexToRgba(theme.orbit.borderColorInputFocus, 15), ";");
  });
};

export default formElementFocus;