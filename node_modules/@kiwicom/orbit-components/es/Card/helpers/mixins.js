import { css } from "styled-components";
import mq from "../../utils/mediaQuery";
import defaultTheme from "../../defaultTheme";
import { getBorder } from "./borders";
export var CardElement = css(["width:100%;box-sizing:border-box;position:relative;box-shadow:", ";border-top:", ";background:", ";", ""], function (_ref) {
  var expanded = _ref.expanded,
      theme = _ref.theme;
  return expanded && theme.orbit.boxShadowActionActive;
}, function (_ref2) {
  var expanded = _ref2.expanded;
  return !expanded && getBorder;
}, function (_ref3) {
  var theme = _ref3.theme;
  return theme.orbit.backgroundCard;
}, mq.largeMobile(css(["border-left:", ";border-right:", ";"], function (_ref4) {
  var expanded = _ref4.expanded;
  return !expanded && getBorder;
}, function (_ref5) {
  var expanded = _ref5.expanded;
  return !expanded && getBorder;
})));
CardElement.defaultProps = {
  theme: defaultTheme
};