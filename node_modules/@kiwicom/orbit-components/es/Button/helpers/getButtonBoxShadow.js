import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";

var _opacity;

import convertHexToRgba from "@kiwicom/orbit-design-tokens/lib/convertHexToRgba";
import { TOKENS, TYPE_OPTIONS } from "../consts";
import { BUTTON_STATES } from "../../primitives/ButtonPrimitive/common/consts";
import getButtonTypeToken from "./getButtonTypeToken";
var opacity = (_opacity = {}, _defineProperty(_opacity, TYPE_OPTIONS.PRIMARY, 15), _defineProperty(_opacity, TYPE_OPTIONS.SECONDARY, 8), _defineProperty(_opacity, TYPE_OPTIONS.CRITICAL, 15), _defineProperty(_opacity, TYPE_OPTIONS.WHITE, 8), _defineProperty(_opacity, TYPE_OPTIONS.PRIMARY_SUBTLE, 8), _defineProperty(_opacity, TYPE_OPTIONS.CRITICAL_SUBTLE, 8), _opacity);

var getButtonBoxShadow = function getButtonBoxShadow(state, disabled, theme, type) {
  var wrappedButtonTypeToken = function wrappedButtonTypeToken(name) {
    return getButtonTypeToken(name, type, theme);
  };

  if (disabled) {
    return null;
  }

  if (state === BUTTON_STATES.ACTIVE) {
    return "inset 0 0 6px 3px ".concat(convertHexToRgba(theme.orbit.paletteInkNormal, opacity[type]), ";");
  }

  if (state === BUTTON_STATES.FOCUS) {
    return "0 0 0 3px ".concat(wrappedButtonTypeToken(TOKENS.borderColorButtonFocus));
  }

  return null;
};

export default getButtonBoxShadow;