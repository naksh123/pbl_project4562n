import { css } from "styled-components";
import { ALIGNS } from "../consts";

var resolvePopoverHorizontal = function resolvePopoverHorizontal(_ref) {
  var anchor = _ref.anchor,
      containerLeft = _ref.containerLeft,
      containerWidth = _ref.containerWidth,
      popoverWidth = _ref.popoverWidth,
      offset = _ref.offset;

  if (anchor === ALIGNS.START) {
    return css(["left:", "px;"], Math.floor(containerLeft + offset.left));
  }

  if (anchor === ALIGNS.CENTER) {
    return css(["left:", "px;"], Math.floor(containerLeft + containerWidth / 2 - popoverWidth / 2 + offset.left));
  }

  if (anchor === ALIGNS.END) {
    return css(["left:", "px;"], Math.floor(containerLeft + containerWidth - popoverWidth + offset.left));
  }

  return null;
};

export default resolvePopoverHorizontal;