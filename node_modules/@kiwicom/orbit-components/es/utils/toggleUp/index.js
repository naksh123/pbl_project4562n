import { keyframes } from "styled-components";

var toggleUp = function toggleUp(contentHeight) {
  return keyframes(["0%{max-height:", "px;opacity:1}100%{max-height:0;opacity:0}"], contentHeight);
};

export default toggleUp;