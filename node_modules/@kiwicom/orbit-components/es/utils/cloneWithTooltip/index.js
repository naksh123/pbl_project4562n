import { cloneElement } from "react";

var cloneWithTooltip = function cloneWithTooltip(tooltip, children) {
  if (!tooltip) return children;
  return /*#__PURE__*/cloneElement(tooltip, {
    children: children
  });
};

export default cloneWithTooltip;