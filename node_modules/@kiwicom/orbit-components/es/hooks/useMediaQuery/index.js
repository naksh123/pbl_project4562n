import * as React from "react";
import QueryContext from "../../ThemeProvider/QueryContext";
import useMediaQueryContext from "../../ThemeProvider/QueryContext/useMediaQueryContext";

var useMediaQuery = function useMediaQuery() {
  var media = React.useContext(QueryContext);
  var value = useMediaQueryContext();

  if (media != null && Object.values(media).some(function (v) {
    return v != null;
  })) {
    return media;
  }

  return value;
};

export default useMediaQuery;